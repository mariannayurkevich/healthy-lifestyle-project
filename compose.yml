services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_DB=fitness_app
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USER}
    ports:
      - "5432:5432"